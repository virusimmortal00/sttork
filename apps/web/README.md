# Web application

This package will contain the voice-first PWA and its minimal, accessible, and
debug event projections. M0 defines only the package and trust boundary; product
UI work begins after the deterministic engine and guide contracts exist.
